services:
  - type: web
    name: springboot-todo-api
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: todo-postgres
          property: connectionString

databases:
  - name: todo-postgres
    plan: free
    databaseName: todo_db
    user: todo_user
