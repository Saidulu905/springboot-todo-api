services:
  - type: web
    name: springboot-todo-api
    runtime: docker
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: todo-postgres
          property: connectionString

databases:
  - name: todo-postgres
    databaseName: todo_db
    user: todo_user
